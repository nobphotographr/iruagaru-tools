RewriteEngine On

# tools.iruagaru.com 以外から同じ公開フォルダを直接参照させない。
RewriteCond %{HTTP_HOST} !^tools\.iruagaru\.com$ [NC]
RewriteRule ^ - [F,L]

# サブドメインの反映後は、すべてのツールをHTTPSへ統一する。
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://tools.iruagaru.com%{REQUEST_URI} [R=301,L]

Options -Indexes
